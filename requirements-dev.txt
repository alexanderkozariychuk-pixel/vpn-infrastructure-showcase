# Tooling and test-only dependencies — not installed into the runtime image.
#
# The suite exercises real application code, so it also needs what the
# application needs: install pwa/requirements.txt alongside this file.
pytest==8.3.4
bandit[toml]==1.8.0
pip-audit==2.7.3
ruff==0.8.4

# Async SQLite driver, tests only. Production is PostgreSQL through asyncpg;
# this lets the subscription and billing tests run against a real database
# instead of a mock, with no service to stand up in CI.
aiosqlite==0.22.1

# The favicon tests open the icon files to check sizes and measure ink at
# 16 px, as does pwa/static/icons/make_favicon.py. Missing from this file for
# three days: the suite passed on a machine with Pillow installed system-wide
# and failed in CI, which installs nothing it is not told to.
Pillow==12.3.0
